apiVersion: v1
kind: ServiceAccount
metadata:
  name: __PROJECT_SLUG__
automountServiceAccountToken: false
---
apiVersion: v1
kind: Service
metadata:
  name: __PROJECT_SLUG__
spec:
  selector:
    app.kubernetes.io/name: __PROJECT_SLUG__
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: __PROJECT_SLUG__
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: __PROJECT_SLUG__
  template:
    metadata:
      labels:
        app.kubernetes.io/name: __PROJECT_SLUG__
    spec:
      serviceAccountName: __PROJECT_SLUG__
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: app-image
          ports:
            - name: http
              containerPort: 8080
          env:
            - {name: OTEL_SERVICE_NAME, value: __PROJECT_SLUG__}
            - {name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://otel-collector.observability.svc.cluster.local:4318"}
          resources:
            requests: {cpu: 25m, memory: 32Mi}
            limits: {cpu: 150m, memory: 128Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: ["ALL"]}
            readOnlyRootFilesystem: true
          startupProbe:
            httpGet: {path: /, port: http}
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: {path: /, port: http}
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /, port: http}
            periodSeconds: 20
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
      volumes:
        - name: tmp
          emptyDir: {sizeLimit: 32Mi}
