# __PROJECT_NAME__

Minimal ASP.NET Core API template for the local cloud dev platform.

```powershell
./scripts/restore.ps1
./scripts/test.ps1
./scripts/run-local.ps1
```

The API listens on `http://localhost:8080` in Docker and exposes `/health`.
