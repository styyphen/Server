# node-api

Minimal Node.js API template for the local cloud dev platform.

```powershell
./scripts/restore.ps1
./scripts/test.ps1
./scripts/run-local.ps1
```

The API listens on `http://localhost:8080` by default and exposes `/health`.

