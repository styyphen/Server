{
  "annotations": {
    "list": []
  },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum by (severity) (ALERTS{alertstate=\"firing\"})",
          "legendFormat": "{{severity}}"
        }
      ],
      "title": "Firing Alerts By Severity",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum by (severity) (ALERTS{alertstate=\"pending\"})",
          "legendFormat": "{{severity}}"
        }
      ],
      "title": "Pending Alerts By Severity",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [
        {
          "expr": "count(ALERTS{alertstate=~\"pending|firing\"})",
          "legendFormat": "active alerts"
        }
      ],
      "title": "Active Alerts",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 12, "x": 0, "y": 7 },
      "id": 5,
      "targets": [
        {
          "expr": "sum by (failure_cause) (ALERTS{alertstate=~\"pending|firing\"})",
          "legendFormat": "{{failure_cause}}"
        }
      ],
      "title": "Active Alerts By Failure Cause",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 12, "x": 12, "y": 7 },
      "id": 6,
      "targets": [
        {
          "expr": "sum by (alert_category) (ALERTS{alertstate=~\"pending|firing\"})",
          "legendFormat": "{{alert_category}}"
        }
      ],
      "title": "Active Alerts By Category",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 14 },
      "id": 4,
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "editorMode": "code",
          "expr": "ALERTS{alertstate=~\"pending|firing\"}",
          "format": "table",
          "instant": true,
          "range": false,
          "refId": "A"
        }
      ],
      "title": "Active Alert Details",
      "type": "table"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 40,
  "tags": ["local-observability", "alerts"],
  "time": { "from": "now-1h", "to": "now" },
  "timezone": "browser",
  "title": "Alerts Overview",
  "uid": "alerts-overview",
  "version": 3
}
